# Codex-O AI 协作规范

本文件是项目级 AI 执行规范。进入本目录工作的 AI 必须先阅读本文件，再阅读
`PROGRESS.md`、`BLOCKED.md` 和当前任务对应的产品文档。

## 1. 项目目标

Codex-O 是本机优先的 Tauri 桌面应用，包含两个能力域：

1. Skill Intelligence & Management：发现、解析、解释、比较和安全管理 Codex Skill。
2. Codex Sessions & Usage：预览、下载、安全删除 Codex 会话并分析 Token 使用情况。

正式发布目标是 `v1.0`。开发顺序固定为：

```text
T0 工程与兼容基线
  -> M1 看见与看懂
  -> M2 可控管理
  -> M3 会话管理与使用洞察
  -> v1.0 稳定发布
```

未经文档评审，不允许通过代码改变阶段顺序或产品边界。

## 2. 权威文档

| 优先级 | 文件 | 职责 |
|---|---|---|
| 1 | `doc/产品蓝图与领域模型.md` | 产品范围、领域对象和安全边界的唯一上游基线 |
| 2 | `doc/需求文档.md` | 用户需求、功能范围和验收指标 |
| 3 | `doc/技术设计文档.md` | 架构、数据模型、IPC、安全和测试设计 |
| 4 | `doc/实施计划.md` | 阶段、任务编号、依赖、估算和交付顺序 |
| 5 | `prototype/` | 交互和视觉参考，不是功能范围承诺 |

发生冲突时必须停止受影响的实现，把问题写入 `BLOCKED.md`。先修正文档，再继续代码。

## 3. 每次任务的固定流程

### 3.1 开工前

1. 阅读 `PROGRESS.md`，确认当前里程碑、当前任务和下一任务。
2. 阅读 `BLOCKED.md`，不得绕过未解决的阻塞项。
3. 阅读当前任务涉及的权威文档章节。
4. 检查工作区现状，不假设文档中的“已完成”已经落地。
5. 在对话中说明本步目标、改动范围、关键假设和验证方式。
6. 将任务状态改为“进行中”，在 `PROGRESS.md` 写开工记录后再改文件。

### 3.2 执行中

- 一次只推进一个可验证的逻辑步骤。
- 每完成一个逻辑步骤，必须立即：
  1. 更新 `PROGRESS.md`。
  2. 在对话中给出不超过 6 行的步骤总结。
  3. 总结必须包含：完成内容、改动文件、验证结果、当前进度、下一步、风险。
- 任务较长时至少每 30 分钟提供一次进展更新。
- 发现无关问题只记录到 `BLOCKED.md`，不得顺手修改。
- 同一验证连续失败 3 次，停止该项并记录原始错误、已尝试方案和建议动作。

### 3.3 收工前

1. 运行最贴近改动点的测试、lint、类型检查或数据校验。
2. 检查无关格式变化、原注释丢失和越界文件修改。
3. 更新 `PROGRESS.md` 的完成状态、实际验证和下一任务。
4. 更新 `BLOCKED.md`；没有阻塞时明确写“无”。
5. 最终报告必须包含：改了什么、为什么、验证结果、剩余风险、下一任务。

只说“已完成”不算完成。没有实际验证输出的任务不得标记为完成。

## 4. 进度计算规则

- 工程总基线为 `83 人天`，来源为 `doc/实施计划.md`。
- 阶段进度按已完成任务的人天占阶段总人天计算，不使用主观百分比。
- 尚未编码的任务记为 `0%`，已有原型不能算作应用实现。
- 部分完成的任务不折算人天，除非实施计划已拆出可独立验收的子任务。
- 产品文档、原型和工程实现分别统计，禁止混成一个虚假的总体百分比。
- 计划日期过期但任务未完成时，标记“逾期未启动/逾期进行中”，不得自动顺延并假装正常。

## 5. 变更记录格式

`PROGRESS.md` 的每条步骤记录必须使用：

```text
### YYYY-MM-DD HH:mm - <任务编号> <步骤名称>
- 状态：进行中 / 完成 / 阻塞
- 改动：文件和行为
- 原因：为什么做
- 验证：实际命令及结果
- 进度：阶段完成度
- 下一步：唯一下一动作
- 风险：无，或具体风险
```

禁止在 `AGENTS.md` 记录开发历史。历史只进入 `PROGRESS.md`，稳定后的事实更新到对应权威文档。

## 6. 工程边界

- 桌面框架：Tauri 2。
- 前端：React + TypeScript + Vite。
- 本地后端：Rust。
- 自有数据库：SQLite，位置设计为 `~/.codex/codex-o/data.db`。
- 密钥：系统安全存储，不进入 SQLite、前端状态或普通日志。
- 前端不得直接访问任意文件路径、Codex SQLite 或系统 Keyring。
- 文件、SQLite、路径校验和写操作必须位于 Rust 安全边界内。
- 列表 DTO 不返回用户绝对路径、API Key、Prompt、会话正文或 confirmation token 历史。
- 使用结构化解析器和类型，不用字符串拼接模拟 YAML、Markdown AST、JSONL 或 SQL schema。

## 7. 产品不变量

### 7.1 Skill

- Skill 必须通过 `SkillProvider` 标识来源；`name` 不是主键。
- 事实由确定性解析器提取，AI 只负责解释，不得猜测文件、依赖、权限或路径。
- AI 结果是可追溯的 `SkillPassport`，必须包含证据、置信度和不确定性。
- User、Repo、System、Plugin、Bundled 和 Additional Root 的权限由 Provider 能力决定。
- 除受管 User Skill 外默认只读。
- “卸载”默认实现为 quarantine；不得直接递归删除活跃 Skill。
- 所有写操作必须使用 `plan -> confirm -> execute`。

### 7.2 会话与 Token

- 会话列表、预览、搜索和统计使用只读连接。
- 完整对话按 rollout JSONL 原顺序展示用户和助手的每一条可见消息。
- 默认隐藏 developer、reasoning、工具调用和工具输出。
- 原始下载必须保留 rollout JSONL，不把预览内容伪装成原始文件。
- 会话删除是 M3 唯一允许写 Codex 状态库的能力。
- 删除前必须检查 schema、活跃状态、rollout hash、更新时间、关联表和 Subagent。
- 删除必须备份、显式处理关联行、使用 SQLite 事务并支持双向回滚。
- 不依赖 `ON DELETE CASCADE`；必须覆盖 `PRAGMA foreign_keys=0`。
- 未知关联表或 schema 不兼容时关闭删除，列表和预览仍可使用。
- Token 只有总量时不得伪造输入/输出拆分，也不得给出账单级费用。

## 8. 安全与隐私

- 禁止提交密钥、令牌、Authorization、真实会话正文或本机隐私数据。
- 日志不得记录 Prompt、AI 原始响应、Skill 正文、会话正文和用户绝对路径。
- 真实 Codex 数据只用于只读查询、预览和下载测试。
- 删除、恢复和永久清理只能使用隔离 fixture，除非用户明确授权真实数据操作。
- 外部输入必须校验路径、symlink、大小、编码和错误路径。
- 破坏性操作、批量删除、权限修改和不可恢复迁移必须先获得用户明确授权。

## 9. 代码与测试

- 先读现有代码和测试，再做最小范围修改。
- 保持现有格式和注释；不做无关重构、整理或依赖升级。
- 新增复杂逻辑必须写清楚原因、边界和失败路径的注释。
- Rust 负责业务安全和数据一致性；前端不重复实现权限判断。
- 测试覆盖正常、异常、只读、损坏输入、并发变化和回滚路径。
- 禁止 `skip`、`todo`、删除测试、放宽断言、mock 被测主体、修改阈值或使用 `|| true` 制造假绿。
- 测试数和覆盖率不得低于修改前基线。
- 前端项目不自行启动、构建或打包，只执行静态检查并说明启动、构建、打包命令。

## 10. Git 与文件操作

- 未经用户明确要求，不执行 `git add`、`git commit`、`git push`、创建分支或 PR。
- 不使用 `git reset --hard`、`git checkout --` 或其他会覆盖用户改动的命令。
- 工作区存在未知改动时视为用户改动，必须保留并与其协作。
- 手工编辑使用补丁方式，禁止批量重排无关文件。
- `prototype/` 是完整原型交付包，目录和文件保持原位，不拆分到项目根目录。
- 进入 `prototype/` 工作时必须继续读取 `prototype/AGENTS.md`。
- 不修改 `prototype.zip`；只有用户明确要求重新导出原型包时才能更新。
- `prototype/` 仅在明确的原型任务中修改；正式应用实现只能参考其信息架构、令牌和交互，不得直接复制原型脚本。

## 11. 当前执行入口

- 当前状态和下一任务：`PROGRESS.md`
- 待裁决与阻塞：`BLOCKED.md`
- 当前首个工程任务：`T0-01 工程初始化`
- 每个新会话都从读取这三个文件开始，不得直接凭聊天记忆继续开发。
