# Codex-O 阻塞与待裁决

**基线差异原始输出**：`head=14334b742369d51c8c77512cd9db576c1a7f1975`；`origin=14334b742369d51c8c77512cd9db576c1a7f1975`；任务书预期 origin 为 `4217a71`。

**基线差异原始输出**：`head=4217a711f9d29d9d588a5ffe7c816285585bfde1`；`origin=4217a711f9d29d9d588a5ffe7c816285585bfde1`；任务书预期 origin 为 `7fb46b7`。

**更新时间**：2026-07-27 21:14

本文件只记录会阻止任务继续或改变产品、技术、安全边界的问题。普通待办写入
`PROGRESS.md`，稳定结论写入对应权威文档。

## 当前阻塞

无。

T0 Gate 收工结论：无。

M1-S1/M1-S2 收工结论：无。

### B-004 T0-04 远端基线已前进

- 状态：已解除
- 问题：任务书预期 `origin/main` 为 `4217a71`，实测已前进到 `14334b7`。
- 证据：本地 HEAD 与 `origin/main` 均为 `14334b742369d51c8c77512cd9db576c1a7f1975`，工作区变更数为 0。
- 影响：仅使“本地领先 1 个提交”的描述失效；源码、测试和保护指纹基线不变。
- 已尝试：停止远端 Git 操作，复跑 Rust 25 tests、前端 1 test 和保护指纹前置项。
- 建议：以共同提交 `14334b7` 作为 T0-04 实际基线继续本地工作。
- 解除条件：已满足；本地与远端提交一致且工作区干净。

### B-003 T0-03 远端基线已前进

- 状态：已解除
- 问题：任务书预期 `origin/main` 为 `7fb46b7`，实测已前进到 `4217a71`。
- 证据：本地 HEAD 与 `origin/main` 均为 `4217a711f9d29d9d588a5ffe7c816285585bfde1`，工作区变更数为 0。
- 影响：仅使“本地领先 1 个提交”的描述失效；源码、测试和保护指纹基线不变。
- 已尝试：停止远端 Git 操作，复跑 Rust 12 tests、前端 1 test 和保护指纹。
- 建议：以共同提交 `4217a71` 作为 T0-03 实际基线继续本地工作。
- 解除条件：已满足；本地与远端提交一致且工作区干净。

### B-001 Git 回滚与检查点策略未授权

- 状态：已解除
- 问题：当前不是 Git 仓库，AI 也没有初始化 Git 和创建本地提交的授权。
- 证据：`git status` 返回 `fatal: not a git repository`。
- 影响：T0-01 无法完整交付；长任务没有可靠检查点和回滚基线。
- 已尝试：仅完成文档综合 hash 冻结，没有执行任何 Git 操作。
- 建议：允许 `git init`；允许 AI 在验证全绿后创建本地提交；仍禁止自动 push、分支和 PR。
- 解除条件：已满足；本目标明确授权 Git 初始化和全绿后的一个中文本地提交。

### B-002 启动冒烟验证规则冲突

- 状态：已解除
- 问题：T0-01 要求 macOS 开发模式可启动，但项目规范禁止 AI 自行启动前端。
- 证据：`doc/实施计划.md` 的 T0-01 完成标准与根目录 `AGENTS.md` 第 9 节不一致。
- 影响：AI 无法亲自证明 Tauri 空壳可以启动，可能出现静态检查通过但应用不可运行。
- 已尝试：确认官方 Tauri 2 支持使用 `npm run tauri dev` 启动开发应用。
- 建议：允许 AI 使用带超时、无真实数据写入的 `npm run tauri dev` 做 T0/M1 冒烟；继续禁止打包和发布。
- 解除条件：已满足；本目标明确授权限时运行 Vite 和 `npm run tauri dev`，验证后关闭全部子进程。

## 待裁决

### D-001 开发模式

- 状态：本目标已授权
- 问题：后续按单人开发还是两名工程师执行？
- 默认：未确认前按单人串行执行，不并行 M1、M2、M3。
- 影响：改变日历周期，不改变 83 人天估算和阶段顺序。
- 解除条件：用户明确开发模式。

### D-002 Git 初始化

- 状态：本目标已授权
- 问题：是否允许在当前目录执行 `git init`？
- 默认：未明确授权前不初始化、不暂存、不提交。
- 影响：T0-01 的 Git 和 CI 工作不能标记为完整完成。
- 解除条件：用户明确允许或拒绝初始化 Git。

### D-003 文档基线评审

- 状态：本目标已授权作为开发基线
- 问题：四份 `v1.0-draft` 文档是否通过正式评审？
- 默认：继续作为开发参考，但保持 `draft` 状态。
- 影响：可以进行 T0 技术验证，不得宣称产品范围已经正式冻结。
- 解除条件：用户确认通过，或提出需修改的范围。

### D-004 Rust 工具链管理

- 状态：本目标已授权
- 问题：当前 Rust/Cargo 来自 Homebrew，系统没有 `rustup`。
- 默认：T0-01 先使用现有稳定工具链，不擅自切换安装来源。
- 影响：macOS 单目标开发不受阻；后续 Windows/Linux target、版本固定和组件管理可能不方便。
- 解除条件：T0-01 技术验证确认现有工具链足够，或用户允许迁移到 rustup。

## 新增记录模板

```text
### B-XXX 标题

- 状态：阻塞 / 待确认 / 已解除
- 问题：具体事实
- 证据：命令、错误或文档位置
- 影响：被阻止的任务
- 已尝试：最多三项
- 建议：推荐处理方式
- 解除条件：可验证条件
```
