# Codex-O 阻塞与待裁决

**基线差异原始输出**：`status=* main...origin/main [ahead 1]（clean — nothing to commit）`；`head=555c7e00dc299ddc414930e049beb25d5c104f11`；`origin=7953de60787ddddb84dc5044f09bc08743033532`；任务书预期 `HEAD=origin/main=7953de6`。领先提交仅含 `PROGRESS.md` 的 M2-S3.6 开工记录，未改变受控功能源码。

**基线差异原始输出**：`status=* main...origin/main（clean — nothing to commit）`；`head=246bcb7531c2e7aff48d3fdeb1f405bd37b145a5`；`origin=246bcb7531c2e7aff48d3fdeb1f405bd37b145a5`；任务书预期 `origin/main=d04bb5f` 且 main ahead 1。

**基线差异原始输出**：`head=14334b742369d51c8c77512cd9db576c1a7f1975`；`origin=14334b742369d51c8c77512cd9db576c1a7f1975`；任务书预期 origin 为 `4217a71`。

**基线差异原始输出**：`head=4217a711f9d29d9d588a5ffe7c816285585bfde1`；`origin=4217a711f9d29d9d588a5ffe7c816285585bfde1`；任务书预期 origin 为 `7fb46b7`。

**更新时间**：2026-07-30 09:17

本文件只记录会阻止任务继续或改变产品、技术、安全边界的问题。普通待办写入
`PROGRESS.md`，稳定结论写入对应权威文档。

## 当前阻塞

无。

## 已解除

### B-010 M2-S3.6 进度状态与校验器冲突

- 状态：已解除
- 问题：任务进行中时，M2、M2-S3、M2-S3.6 同时标为“进行中”会被只读校验器按表格顺序计为三个活动叶子。
- 证据：收工后 M2-S3 与 M2-S3.6 均为已完成，M2 保持唯一进行中节点；`node scripts/check-progress.mjs PROGRESS.md` 通过。
- 影响：仅影响中间进度门禁；不影响隔离文件或 SQLite 一致性实现。
- 已尝试：未修改受保护校验器；按任务书完成状态更新后复跑进度检查。
- 建议：后续长任务的中间检查以此校验器的单一活动叶子规则为准。
- 解除条件：已满足。

### B-009 M2-S3 双视口人工验收待确认

- 状态：已解除
- 问题：自动化门禁已经通过，但隔离区是本轮新增页面；此前尚未获得实际 Tauri 窗口下的确认。
- 证据：用户提供 Skills 列表实际 Tauri 截图并确认“没有问题”；按本轮约定，`1280×800`、`1024×700` 下详情隔离按钮与隔离区空态均通过。
- 影响：M2-S3 可以完成收口，下一节点为 M2-S4。
- 已尝试：Rust 248 passed、前端 51 passed、fmt、Clippy、静态检查与两项实现级红→绿反证均已完成。
- 建议：继续 M2-S4 GitHub 安装。
- 解除条件：已满足。

### B-008 M2-S2 Install 路由接入超出白名单

- 状态：已解除
- 问题：上一目标的白名单未包含 `src/App.tsx`，而现有 `/install` 只能渲染通用 `PageState`。
- 证据：本目标授权最小修改后，`src/App.tsx` 从既有 manifest 取得 Install 路由并渲染现有 `InstallPage`；新增路由测试 1 passed，前端全量 42 passed。
- 影响：`/install` 已可访问真实 plan → confirm → execute 前端入口；未新增一级路由，前端未接触目标路径。
- 已尝试：临时恢复通用 `RoutePage` 时路由测试因找不到“本地导入”退出 1；恢复接线后通过。1280×800、1024×700 无横向溢出，用户提供两张 Tauri 实窗截图确认页面完整；开发进程已关闭。
- 建议：下一节点 M2-S3 Quarantine 与恢复。
- 解除条件：已满足。

### B-007 M1 双视口桌面视觉验收

- 状态：已解除
- 问题：此前未取得 1280×800 与 1024×700 的可靠桌面视觉证据。
- 证据：用户在实际 Tauri 窗口中逐项核对并明确报告：1280×800 与 1024×700 下的 Skill 列表、无 AI 详情、Skill 对比、设置页面、错误降级和返回列表全部通过。
- 影响：M1-GATE 和 M1 可以按实际证据标记为完成。
- 已尝试：停止无进展的自动化 UI 路径，改由用户在两个精确视口下完成手工验收。
- 建议：继续 `M2-S1 三段式操作框架`。
- 解除条件：已满足；两个目标视口的所需检查均通过，相关进程已关闭。

### B-006 M1-S5/M1-S6 远端基线已前进

- 状态：已解除
- 问题：任务书预期 `HEAD=246bcb7`、`origin/main=d04bb5f` 且 main ahead 1，实测本地与远端均为 `246bcb7531c2e7aff48d3fdeb1f405bd37b145a5`。
- 证据：工作区初始状态 clean；HEAD 与目标源码提交一致，仅 `origin/main` 已前进。
- 影响：停止远端 Git 操作；不影响以同一源码提交继续本地实现和验证。
- 已尝试：只读复核工作区、HEAD 和 `origin/main`，未执行 fetch、pull、reset、checkout 或其他远端操作。
- 建议：以共同提交 `246bcb7` 作为本目标实际源码基线，继续本地白名单内工作。
- 解除条件：已满足；本地和远端源码一致且工作区初始干净。

### B-005 Codex 配置目录数据库冲突

- 状态：已解除
- 问题：旧设计把 Codex-O 自有数据库放入 `~/.codex/codex-o`，与现有 Codex 配置数据 version 2 冲突。
- 证据：旧文件只读核对为 version 2；新实现使用 Tauri `app_local_data_dir()` 解析独立 `data.db` 和偏好文件，路径测试、Rust 94 tests 与前端 17 tests 通过。
- 影响：Codex-O 不再读取、写入、迁移或删除旧 `~/.codex/codex-o` 文件；该文件不再阻断自有 Skill 索引。
- 已尝试：先实现 v2→v3 保留式迁移，再按用户确认改为应用专属数据目录隔离；未读取旧表行内容、未写入真实旧文件。
- 建议：旧文件保持原样；只有用户未来明确要求时，才设计受控的旧数据导入流程。
- 解除条件：已满足；自有状态与 Codex 配置目录已分离。

T0 Gate 收工结论：无。

M1-S1/M1-S2 收工结论：无。

M1-S3 收工结论：无。

M1-S3.6 性能修复收工结论：无。

M1-S3.7 Skill 索引与快照持久化收工结论：B-005 已解除，新路径首次手动扫描后可持久化。

M1-S3.8 插件与内置扫描偏好收工结论：无新增阻塞；B-005 状态不变。

M1-S3.9 首次手动后台扫描收工结论：无。

M1-S3.10 自有数据目录隔离收工结论：无。

M1-S4 AI 分析引擎收工结论：无。

M1-S5/M1-S6 收工结论：无。

M1-GATE 设置存储缺陷修复结论：无。

M2-S0 收工结论：无。

M2-S1 收工结论：无。

M2-S2 收工结论：无。

M2-S4 收工结论：无。

M2-S4 取消清理修复结论：无。

M2-S5 官方市场 Provider 收工结论：无。

M2-S5 全量验收稳定性修复结论：无。

### B-004 T0-04 远端基线已前进

- 状态：已解除
- 问题：任务书预期 `origin/main` 为 `4217a71`，实测已前进到 `14334b7`。
- 证据：本地 HEAD 与 `origin/main` 均为 `14334b742369d51c8c77512cd9db576c1a7f1975`，工作区变更数为 0。
- 影响：仅使“本地领先 1 个提交”的描述失效；源码、测试和保护指纹基线不变。
- 已尝试：停止远端 Git 操作，复跑 Rust 25 tests、前端 1 test 和保护指纹前置项。
- 建议：以共同提交 `14334b7` 作为 T0-04 实际基线继续本地工作。
- 解除条件：已满足；本地与远端提交一致且工作区干净。

### B-003 T0-03 远端基线已前进

- 状态：已解除
- 问题：任务书预期 `origin/main` 为 `7fb46b7`，实测已前进到 `4217a71`。
- 证据：本地 HEAD 与 `origin/main` 均为 `4217a711f9d29d9d588a5ffe7c816285585bfde1`，工作区变更数为 0。
- 影响：仅使“本地领先 1 个提交”的描述失效；源码、测试和保护指纹基线不变。
- 已尝试：停止远端 Git 操作，复跑 Rust 12 tests、前端 1 test 和保护指纹。
- 建议：以共同提交 `4217a71` 作为 T0-03 实际基线继续本地工作。
- 解除条件：已满足；本地与远端提交一致且工作区干净。

### B-001 Git 回滚与检查点策略未授权

- 状态：已解除
- 问题：当前不是 Git 仓库，AI 也没有初始化 Git 和创建本地提交的授权。
- 证据：`git status` 返回 `fatal: not a git repository`。
- 影响：T0-01 无法完整交付；长任务没有可靠检查点和回滚基线。
- 已尝试：仅完成文档综合 hash 冻结，没有执行任何 Git 操作。
- 建议：允许 `git init`；允许 AI 在验证全绿后创建本地提交；仍禁止自动 push、分支和 PR。
- 解除条件：已满足；本目标明确授权 Git 初始化和全绿后的一个中文本地提交。

### B-002 启动冒烟验证规则冲突

- 状态：已解除
- 问题：T0-01 要求 macOS 开发模式可启动，但项目规范禁止 AI 自行启动前端。
- 证据：`doc/实施计划.md` 的 T0-01 完成标准与根目录 `AGENTS.md` 第 9 节不一致。
- 影响：AI 无法亲自证明 Tauri 空壳可以启动，可能出现静态检查通过但应用不可运行。
- 已尝试：确认官方 Tauri 2 支持使用 `npm run tauri dev` 启动开发应用。
- 建议：允许 AI 使用带超时、无真实数据写入的 `npm run tauri dev` 做 T0/M1 冒烟；继续禁止打包和发布。
- 解除条件：已满足；本目标明确授权限时运行 Vite 和 `npm run tauri dev`，验证后关闭全部子进程。

## 待裁决

### D-001 开发模式

- 状态：本目标已授权
- 问题：后续按单人开发还是两名工程师执行？
- 默认：未确认前按单人串行执行，不并行 M1、M2、M3。
- 影响：改变日历周期，不改变 83 人天估算和阶段顺序。
- 解除条件：用户明确开发模式。

### D-002 Git 初始化

- 状态：本目标已授权
- 问题：是否允许在当前目录执行 `git init`？
- 默认：未明确授权前不初始化、不暂存、不提交。
- 影响：T0-01 的 Git 和 CI 工作不能标记为完整完成。
- 解除条件：用户明确允许或拒绝初始化 Git。

### D-003 文档基线评审

- 状态：本目标已授权作为开发基线
- 问题：四份 `v1.0-draft` 文档是否通过正式评审？
- 默认：继续作为开发参考，但保持 `draft` 状态。
- 影响：可以进行 T0 技术验证，不得宣称产品范围已经正式冻结。
- 解除条件：用户确认通过，或提出需修改的范围。

### D-004 Rust 工具链管理

- 状态：本目标已授权
- 问题：当前 Rust/Cargo 来自 Homebrew，系统没有 `rustup`。
- 默认：T0-01 先使用现有稳定工具链，不擅自切换安装来源。
- 影响：macOS 单目标开发不受阻；后续 Windows/Linux target、版本固定和组件管理可能不方便。
- 解除条件：T0-01 技术验证确认现有工具链足够，或用户允许迁移到 rustup。

## 新增记录模板

```text
### B-XXX 标题

- 状态：阻塞 / 待确认 / 已解除
- 问题：具体事实
- 证据：命令、错误或文档位置
- 影响：被阻止的任务
- 已尝试：最多三项
- 建议：推荐处理方式
- 解除条件：可验证条件
```
